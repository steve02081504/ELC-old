#include "../forinclude/elc/all"
int main(){
	using namespace elc;
	static_assert(::std::is_convertible_v<setter,comn_ptr::convert_interface>);
	nil.arec(2)==nil.arec(1);
	weak_ptr a=nil.arec(2);
	a==nil.arec(2);
}
